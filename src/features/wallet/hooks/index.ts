/**
 * Wallet feature — hooks.
 *
 * Add the feature's hooks here and re-export them. Consumers import from
 * `@/features/wallet`, which surfaces this barrel.
 */
export { useWallet } from './useWallet';
